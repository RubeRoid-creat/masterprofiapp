# 📦 Настройка базы данных PostgreSQL для "МастерПрофи"

## 📁 Созданные файлы и документация

### 🎯 Основные документы:

1. **`QUICKSTART_POSTGRESQL.md`** - Быстрый старт (5 минут)
   - Краткие команды для настройки PostgreSQL на сервере
   - Минимум текста, максимум действий

2. **`POSTGRESQL_SERVER_SETUP.md`** - Полная инструкция (подробная)
   - Пошаговая настройка PostgreSQL на удаленном сервере
   - Безопасность, резервное копирование, мониторинг
   - Решение проблем и диагностика

3. **`MIGRATIONS_GUIDE.md`** - Руководство по миграциям
   - Как применять миграции
   - Решение проблем с Prisma CDN
   - Откат миграций

### 🛠️ Инструменты:

4. **`website/test-connection.ps1`** - Скрипт диагностики
   - Проверка доступности сервера
   - Проверка порта PostgreSQL
   - Тест подключения к базе данных

5. **`website/scripts/apply-migrations-direct.js`** - Применение миграций без Prisma
   - Прямое выполнение SQL
   - Не требует Prisma engines
   - Обход проблемы с binaries.prismacdn.com

6. **`website/prisma/migrations/001_init.sql`** - SQL миграция
   - Создание всех таблиц для веб-сайта
   - news, prices, forum_topics, forum_replies, contact_messages

---

## 🚀 Быстрый старт (5 шагов)

### Шаг 1: Проверьте текущее состояние
```powershell
cd Z:\BestAPP\website
.\test-connection.ps1
```

**Ожидаемый результат:**
- ✅ Сервер доступен
- ❌ Порт 5432 закрыт (нужна настройка)

### Шаг 2: Подключитесь к серверу
```powershell
ssh root@212.74.227.208
```

Если у вас нет SSH доступа:
- Используйте PuTTY: https://www.putty.org/
- Или запросите доступ у администратора сервера

### Шаг 3: Установите PostgreSQL на сервере

**Для Ubuntu/Debian:**
```bash
sudo apt update && sudo apt install -y postgresql postgresql-contrib
sudo systemctl start postgresql && sudo systemctl enable postgresql
```

**Для CentOS/Rocky Linux:**
```bash
sudo dnf install -y postgresql15-server postgresql15-contrib
sudo /usr/pgsql-15/bin/postgresql-15-setup initdb
sudo systemctl start postgresql-15 && sudo systemctl enable postgresql-15
```

### Шаг 4: Настройте базу данных

```bash
sudo -u postgres psql
```

В консоли PostgreSQL выполните:
```sql
-- Создать базу данных
CREATE DATABASE bestapp_website;

-- Создать пользователя (ЗАМЕНИТЕ ПАРОЛЬ!)
CREATE USER masterprofi WITH PASSWORD 'Ваш_Сложный_Пароль_2024!';

-- Дать права
GRANT ALL PRIVILEGES ON DATABASE bestapp_website TO masterprofi;

-- Подключиться к базе и дать права на схему
\c bestapp_website
GRANT ALL ON SCHEMA public TO masterprofi;
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO masterprofi;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO masterprofi;

\q
```

### Шаг 5: Настройте удаленный доступ

**A. Разрешить прослушивание на всех интерфейсах:**
```bash
# Найти версию PostgreSQL
ls /etc/postgresql/

# Редактировать конфиг (замените 15 на вашу версию)
sudo nano /etc/postgresql/15/main/postgresql.conf

# Найти строку и изменить:
listen_addresses = '*'

# Сохранить: Ctrl+O, Enter, Ctrl+X
```

**B. Разрешить подключения с вашего IP:**
```bash
sudo nano /etc/postgresql/15/main/pg_hba.conf

# Добавить в конец файла:
host    all    all    0.0.0.0/0    md5

# Сохранить: Ctrl+O, Enter, Ctrl+X
```

**C. Открыть порт в файрволе:**
```bash
# Для UFW (Ubuntu/Debian)
sudo ufw allow 5432/tcp
sudo ufw reload

# Для firewalld (CentOS/Rocky)
sudo firewall-cmd --permanent --add-port=5432/tcp
sudo firewall-cmd --reload
```

**D. Перезапустить PostgreSQL:**
```bash
# Ubuntu/Debian
sudo systemctl restart postgresql

# CentOS/Rocky
sudo systemctl restart postgresql-15

# Проверить статус
sudo systemctl status postgresql
```

### Шаг 6: Создайте .env на локальной машине

Создайте файл `Z:\BestAPP\website\.env`:
```env
DATABASE_URL="postgresql://masterprofi:Ваш_Сложный_Пароль_2024!@212.74.227.208:5432/bestapp_website"
```

### Шаг 7: Проверьте подключение
```powershell
cd Z:\BestAPP\website
.\test-connection.ps1
```

**Ожидаемый результат:**
- ✅ Сервер доступен
- ✅ Порт 5432 открыт
- ✅ Подключение к базе данных успешно

### Шаг 8: Примените миграции
```powershell
node scripts/apply-migrations-direct.js
```

**Ожидаемый вывод:**
```
[INFO] Connecting to database...
[SUCCESS] Connected to database
[INFO] Reading migration file...
[INFO] Applying migration...
[SUCCESS] Migration applied successfully!
```

### Шаг 9: Проверьте таблицы на сервере

На сервере:
```bash
sudo -u postgres psql -d bestapp_website -c "\dt"
```

**Должны быть созданы:**
- ✅ news
- ✅ prices
- ✅ forum_topics
- ✅ forum_replies
- ✅ contact_messages

---

## 🔐 Важно: Безопасность!

### 1. Смените пароль на сложный:
```sql
ALTER USER masterprofi WITH PASSWORD 'Очень_Сложный_Пароль_2024!@#$%';
```

### 2. Ограничьте доступ по вашему IP:

Узнайте ваш IP:
```powershell
Invoke-RestMethod https://api.ipify.org
```

На сервере в `/etc/postgresql/15/main/pg_hba.conf` замените:
```
host    all    all    0.0.0.0/0    md5
```
На:
```
host    all    all    ВАШ_IP/32    md5
```

Перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

### 3. Настройте автобэкап (рекомендуется):

См. раздел "Резервное копирование" в `POSTGRESQL_SERVER_SETUP.md`

---

## 📊 Что дальше?

### Для веб-сайта (website):
- ✅ База данных настроена
- ✅ Таблицы созданы
- ⏭️ Можно запускать: `npm run dev`

### Для backend:
- ⏭️ Создайте отдельную базу: `bestapp_backend`
- ⏭️ Настройте `.env` в папке `backend`
- ⏭️ Примените миграции backend (если есть)

---

## ❓ Возникли проблемы?

### Проблема: Порт 5432 недоступен после настройки
**Решение:**
1. Проверьте статус PostgreSQL: `sudo systemctl status postgresql`
2. Проверьте файрвол: `sudo ufw status` или `sudo firewall-cmd --list-all`
3. Проверьте `listen_addresses` в postgresql.conf
4. Проверьте логи: `sudo journalctl -u postgresql -f`

### Проблема: Authentication failed
**Решение:**
1. Проверьте пароль в .env файле
2. Убедитесь, что в pg_hba.conf используется метод `md5`
3. Попробуйте сбросить пароль пользователя

### Проблема: Prisma CDN недоступен (binaries.prismacdn.com)
**Решение:**
- Используйте `node scripts/apply-migrations-direct.js` вместо `npx prisma`
- Это обходит необходимость в Prisma engines

### Проблема: Провайдер блокирует порт 5432
**Решение:**
Используйте SSH туннель:
```powershell
ssh -L 5432:localhost:5432 root@212.74.227.208 -N
```
Затем в .env используйте `localhost` вместо IP сервера.

---

## 📚 Дополнительные ресурсы

- **PostgreSQL официальная документация:** https://www.postgresql.org/docs/
- **Prisma документация:** https://www.prisma.io/docs/
- **Node.js pg документация:** https://node-postgres.com/

---

## ✅ Чеклист завершения

- [ ] PostgreSQL установлен на сервере 212.74.227.208
- [ ] База данных `bestapp_website` создана
- [ ] Пользователь `masterprofi` создан с сильным паролем
- [ ] Удаленный доступ настроен (postgresql.conf, pg_hba.conf)
- [ ] Порт 5432 открыт в файрволе
- [ ] Файл .env создан локально
- [ ] Скрипт test-connection.ps1 показывает успех
- [ ] Миграции применены успешно
- [ ] Таблицы созданы (проверено через \dt)
- [ ] Доступ ограничен по IP (безопасность)
- [ ] Настроено автоматическое резервное копирование

---

## 🎉 Поздравляем!

После выполнения всех шагов у вас будет:
- ✅ Полностью настроенная PostgreSQL на удаленном сервере
- ✅ Безопасное удаленное подключение
- ✅ Все необходимые таблицы для веб-сайта
- ✅ Готовая инфраструктура для развертывания приложения

---

**Автор:** Cursor AI  
**Дата создания:** 14.12.2025  
**Версия:** 1.0

