# 📱 НАСТРОЙКА SMS ИНТЕГРАЦИИ

## 🎯 ОБЗОР

SMS интеграция для отправки кодов подтверждения телефонов пользователей.

**Поддерживаемые провайдеры:**
- ✅ **SMS.ru** (рекомендуется для России)
- ✅ **Twilio** (международный fallback)

---

## 🚀 ВАРИАНТ 1: SMS.RU (РЕКОМЕНДУЕТСЯ)

### Преимущества:
- ✅ Российский сервис
- ✅ Низкие цены (~1-3 руб за SMS)
- ✅ Быстрая доставка
- ✅ Не нужны дополнительные npm пакеты
- ✅ Простой REST API

### Шаг 1: Регистрация на SMS.ru

1. Перейдите на https://sms.ru
2. Нажмите "Регистрация"
3. Заполните форму:
   - Email
   - Пароль
   - Телефон (для подтверждения)
4. Подтвердите email и телефон

### Шаг 2: Получение API ключа

1. Войдите в личный кабинет: https://sms.ru/panel
2. Перейдите в раздел "Настройки" → "API"
3. Скопируйте ваш **API ID** (выглядит как: `XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX`)
4. (Опционально) Настройте IP-адреса для доступа к API

### Шаг 3: Пополнение баланса

1. В личном кабинете перейдите в "Финансы" → "Пополнить"
2. Минимальная сумма: 100 руб
3. Способы оплаты:
   - Банковская карта
   - Яндекс.Деньги
   - Банковский перевод
4. Стоимость SMS:
   - ~1-3 руб за SMS (зависит от оператора)
   - Рекомендуется: 500-1000 руб для старта

### Шаг 4: Настройка в .env

```env
# SMS Configuration
SMS_PROVIDER=smsru

# SMS.ru API Key
SMSRU_API_ID=XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX
```

### Шаг 5: Тестирование

```bash
# Проверка баланса
curl "https://sms.ru/my/balance?api_id=YOUR_API_ID&json=1"

# Тестовая отправка SMS
curl "https://sms.ru/sms/send?api_id=YOUR_API_ID&to=79991234567&msg=Test&json=1"
```

---

## 🌍 ВАРИАНТ 2: TWILIO (МЕЖДУНАРОДНЫЙ)

### Преимущества:
- ✅ Международная доставка
- ✅ Высокая надежность
- ✅ Богатый функционал
- ⚠️ Дороже (~$0.05-0.10 за SMS)
- ⚠️ Нужен npm пакет

### Шаг 1: Регистрация на Twilio

1. Перейдите на https://www.twilio.com/try-twilio
2. Зарегистрируйтесь (бесплатный trial: $15 кредитов)
3. Подтвердите email и телефон

### Шаг 2: Получение учетных данных

1. В консоли Twilio (https://console.twilio.com/):
2. Скопируйте:
   - **Account SID**
   - **Auth Token**
3. Получите номер телефона:
   - Перейдите в "Phone Numbers" → "Buy a number"
   - Выберите страну (для России выберите русский номер)
   - Купите номер (или используйте trial номер)

### Шаг 3: Установка npm пакета

```bash
cd backend
npm install twilio
```

### Шаг 4: Настройка в .env

```env
# SMS Configuration
SMS_PROVIDER=twilio

# Twilio настройки
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+12345678901
```

### Шаг 5: Тестирование

```javascript
// test-twilio.js
import twilio from 'twilio';

const client = twilio(
  'ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',
  'your_auth_token'
);

client.messages.create({
  body: 'Test SMS from МастерПрофи',
  from: '+12345678901',
  to: '+79991234567'
}).then(message => console.log('SMS ID:', message.sid));
```

---

## 🔧 НАСТРОЙКА FALLBACK (РЕКОМЕНДУЕТСЯ)

Для максимальной надежности используйте SMS.ru как основной + Twilio как fallback:

```env
# Основной провайдер
SMS_PROVIDER=smsru
SMSRU_API_ID=your_api_id

# Fallback провайдер
TWILIO_ACCOUNT_SID=your_sid
TWILIO_AUTH_TOKEN=your_token
TWILIO_PHONE_NUMBER=your_number
```

**Логика работы:**
1. Попытка отправки через SMS.ru
2. Если ошибка → автоматическая отправка через Twilio
3. Если обе попытки неудачны → ошибка

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Проверка конфигурации

```bash
cd backend
node -e "
import('./services/sms-service.js').then(sms => {
  sms.verifySMSService().then(result => {
    console.log('SMS Service Status:', result);
  });
});
"
```

Должно вывести:
```json
{
  "success": true,
  "message": "SMS.ru сервис работает",
  "provider": "smsru",
  "balance": 123.45
}
```

### 2. Тестовая отправка SMS

**Через API:**
```bash
curl -X POST http://localhost:3000/api/verification-codes/send-phone-code \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"phone": "+79991234567"}'
```

**Через тестовый скрипт:**
```javascript
// test-sms.js
import { sendVerificationSMS } from './services/sms-service.js';

const phone = '+79991234567';
const code = '123456';

sendVerificationSMS(phone, code)
  .then(result => console.log('✅ SMS отправлена:', result))
  .catch(error => console.error('❌ Ошибка:', error.message));
```

### 3. Проверка баланса SMS.ru

```bash
cd backend
node -e "
import('./services/sms-service.js').then(sms => {
  sms.checkSMSRuBalance().then(result => {
    console.log('Баланс:', result.balance, result.currency);
  });
});
"
```

---

## 📊 МОНИТОРИНГ

### Проверка статуса отправленной SMS

```javascript
import { checkSMSStatus } from './services/sms-service.js';

const smsId = '123456-789';

checkSMSStatus(smsId)
  .then(result => console.log('Статус:', result.status))
  .catch(error => console.error('Ошибка:', error));
```

### Логи SMS сервиса

Все SMS операции логируются:

```
📱 [SMS.RU] Отправка SMS на 79991234567...
✅ SMS успешно отправлена на 79991234567, ID: 123456-789
```

При ошибках:
```
❌ [SMS.RU] Ошибка подключения: timeout
🔄 Попытка отправки через Twilio (fallback)...
✅ SMS успешно отправлена через Twilio, ID: SM123...
```

---

## 💰 СТОИМОСТЬ

### SMS.ru (Россия):
| Оператор | Цена за SMS |
|----------|-------------|
| МТС | ~1.5 руб |
| Билайн | ~1.5 руб |
| МегаФон | ~1.5 руб |
| Теле2 | ~1.5 руб |

**Примерные расходы:**
- 100 SMS = ~150 руб
- 1000 SMS = ~1500 руб
- 10000 SMS = ~15000 руб

### Twilio (Международный):
| Страна | Цена за SMS |
|--------|-------------|
| Россия | ~$0.10 |
| США | ~$0.0075 |
| Европа | ~$0.08-0.12 |

**Примерные расходы:**
- 100 SMS = ~$10
- 1000 SMS = ~$100
- 10000 SMS = ~$1000

---

## 🔒 БЕЗОПАСНОСТЬ

### Rate Limiting

SMS отправка уже защищена rate limiting:
- **3 попытки / 10 минут** на номер
- Автоблокировка IP при превышении

### Валидация номеров

Сервис автоматически:
- ✅ Проверяет формат номера (11 цифр)
- ✅ Нормализует номер (8 → 7)
- ✅ Убирает лишние символы

### Защита от спама

Рекомендации:
1. Используйте CAPTCHA перед отправкой SMS
2. Ограничьте количество попыток с одного IP
3. Мониторьте необычную активность
4. Используйте blacklist для спам-номеров

---

## 🐛 TROUBLESHOOTING

### Ошибка: "SMS.ru API ID не настроен"

**Решение:**
```bash
# Проверьте .env
cat .env | grep SMS

# Должно быть:
SMS_PROVIDER=smsru
SMSRU_API_ID=ваш_api_id
```

### Ошибка: "Insufficient funds" (Недостаточно средств)

**Решение:**
1. Проверьте баланс: https://sms.ru/panel
2. Пополните счет минимум на 100 руб

### Ошибка: "Invalid phone number"

**Решение:**
- Формат должен быть: `+79991234567` или `89991234567`
- Проверьте длину: 11 цифр для РФ

### SMS не доходит

**Причины:**
1. Неправильный номер
2. Телефон выключен
3. Проблемы у оператора
4. Номер в черном списке

**Диагностика:**
```bash
# Проверьте статус SMS
node -e "
import('./services/sms-service.js').then(sms => {
  sms.checkSMSStatus('YOUR_SMS_ID').then(result => {
    console.log(result);
  });
});
"
```

### Twilio ошибка: "Unable to create record"

**Решение:**
1. Проверьте, что номер подтвержден (trial аккаунт)
2. Проверьте формат номера: +79991234567 (с +)
3. Проверьте баланс Twilio

---

## 📈 МЕТРИКИ

### До внедрения:
- ❌ SMS не работают в production
- ❌ Только режим разработки
- ❌ Нет фallback механизма

### После внедрения:
- ✅ Production-ready SMS.ru интеграция
- ✅ Twilio fallback
- ✅ Валидация номеров
- ✅ Проверка баланса
- ✅ Мониторинг статусов
- ✅ Детальное логирование

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

**SMS.ru:**
- Сайт: https://sms.ru
- Документация API: https://sms.ru/api
- Личный кабинет: https://sms.ru/panel
- Поддержка: support@sms.ru

**Twilio:**
- Сайт: https://www.twilio.com
- Документация: https://www.twilio.com/docs/sms
- Консоль: https://console.twilio.com
- Поддержка: https://support.twilio.com

---

## ✅ ЧЕКЛИСТ АКТИВАЦИИ

- [ ] Зарегистрироваться на SMS.ru
- [ ] Получить API ID
- [ ] Пополнить баланс (минимум 100 руб)
- [ ] Добавить `SMS_PROVIDER=smsru` в .env
- [ ] Добавить `SMSRU_API_ID=...` в .env
- [ ] Перезапустить сервер: `pm2 restart`
- [ ] Протестировать отправку SMS
- [ ] Проверить баланс
- [ ] (Опционально) Настроить Twilio fallback

---

**Дата создания:** 23 декабря 2025  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
