# ⚠️ ТРЕБУЕТСЯ ПЕРЕЗАПУСК СЕРВЕРА

## Сделаны критические изменения в коде сервера!

Файл `backend/routes/orders.js` был изменен для исправления ошибки 403 при создании заказа.

### 🔴 Что нужно сделать НЕМЕДЛЕННО:

1. **Подключитесь к серверу по SSH:**
```bash
ssh root@212.74.227.208
```

2. **Перейдите в директорию backend:**
```bash
cd ~/masterprofiapp/backend
```

3. **Получите последние изменения:**
```bash
git pull
```

4. **Проверьте список процессов PM2:**
```bash
pm2 list
```

5. **Перезапустите сервер:**

Если процесс называется "server":
```bash
pm2 restart server
```

Если процесса нет в списке:
```bash
pm2 start server.js --name "server"
pm2 save
```

6. **Проверьте логи:**
```bash
pm2 logs server --lines 50
```

7. **Убедитесь, что сервер работает:**
```bash
curl http://localhost:3000/api/version
```

### ✅ Что было исправлено:

1. **Автоматическое создание записи клиента** - если пользователь имеет роль 'client', но записи в таблице `clients` нет, она создается автоматически
2. **Улучшенное логирование** - добавлены логи для отладки процесса создания заказа
3. **Понятные сообщения об ошибках** - пользователь получает детальную информацию о причине ошибки

### 🐛 Проблема, которую это решает:

Ошибка 403 "Недостаточно прав доступа" при попытке создать заказ в клиентском приложении.

### 📱 Изменения в клиентском приложении:

1. Добавлено явное указание роли "client" при регистрации
2. Улучшена обработка ошибок 401/403 с автоматической очисткой токена
3. Добавлено детальное логирование для отладки

---

**⏰ Без перезапуска сервера изменения НЕ вступят в силу!**


