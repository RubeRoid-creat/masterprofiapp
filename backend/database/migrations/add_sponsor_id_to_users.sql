-- Миграция: добавление колонки sponsor_id в таблицу users
-- Если колонка уже существует, миграция будет проигнорирована

-- Проверяем и добавляем колонку sponsor_id, если её нет
-- SQLite не поддерживает IF NOT EXISTS для ALTER TABLE ADD COLUMN
-- Поэтому используем проверку через PRAGMA table_info

-- Добавляем колонку sponsor_id, если её нет
-- В SQLite нужно использовать ALTER TABLE без проверки, но игнорировать ошибку если колонка уже есть




