# Быстрая инструкция по загрузке Backend на сервер

## 📦 Шаг 1: Создание архива

```bash
cd backend
npm run package
```

Создастся файл `backend-deploy.zip` в папке backend.

## 📤 Шаг 2: Загрузка на сервер

### Вариант A: Через SCP (SSH)

```bash
scp backend-deploy.zip user@your-server.com:/home/user/
```

### Вариант B: Через FTP/SFTP

Используйте FileZilla, WinSCP или другой FTP клиент для загрузки `backend-deploy.zip` на сервер.

## 🚀 Шаг 3: Развертывание на сервере

Подключитесь к серверу через SSH:

```bash
ssh user@your-server.com
```

Выполните команды:

```bash
# Распакуйте архив
unzip backend-deploy.zip -d bestapp-backend
cd bestapp-backend

# Установите зависимости
npm install --production

# Создайте файл .env
cp .env.example .env
nano .env  # Отредактируйте, особенно JWT_SECRET!

# Инициализируйте базу данных
npm run init-db

# Загрузите тестовые данные (опционально)
npm run seed

# Запустите сервер через PM2
npm install -g pm2
pm2 start server.js --name bestapp-backend
pm2 save
pm2 startup
```

## ✅ Проверка

```bash
# Проверьте логи
pm2 logs bestapp-backend

# Проверьте статус
pm2 status

# Проверьте API
curl http://localhost:3000/api/orders
```

## 📝 Важно!

1. **Измените JWT_SECRET** в `.env` на случайную строку
2. **Загрузите firebase-service-account.json** отдельно (если используется)
3. **Настройте firewall** для открытия порта 3000 (или используйте Nginx)
4. **Настройте автозапуск** через PM2 или systemd

Подробная инструкция: см. `DEPLOY.md`

